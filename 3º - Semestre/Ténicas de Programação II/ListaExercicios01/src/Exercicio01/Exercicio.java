package Exercicio01;

public class Exercicio {

	/**
	 * 1. O que é uma variável? Descreva como é feita a declaração de variável
	 *  e explique em termos de memória sua relação.
	 */
	
	/**
	 * A variável é um espaço alocado na memória que podemos atribuir valores.
	 * 
	 * Essa variável pode ser de tipos diferentes, por exemplo:
	 *  String - Atribuímos letras, números e Caracteres. Usa de 4 a 8 bytes da memória. 
	 *  int - Tipo numérico inteiro (0, 1, 200, -15). Usa 4 bytes da memória.
	 *  double - Tipo numérico flutuante (0.20, 0.5). Usa 8 bytes da memória.
	 *  boolean - Tipo comparação (true ou false). Usa 1 byte da memória.
	 *  
	 *  String: String é uma classe em Java. 
	 *  Variáveis do tipo String guardam referências a objetos, e não um valor, 
	 *  como acontece com os tipos primitivos.
	 *  
	 *  int: Inteiros não são objetos (um inteiro é um tipo primitivo) 
	 *  e a maioria das classes úteis requisitam objetos em seus métodos. 
	 *  Assim, para reutilizá-los, você precisa ``envolver'' os inteiros 
	 *  em uma instância da classe Integer. Constrói um objeto Integer, 
	 *  com valor inicial value .
	 *  
	 *  double: As variáveis do tipo double representam números de ponto 
	 *  flutuante de precisão dupla, onde precisam duas vezes a quantidade 
	 *  de memória das variáveis float fornecendo 15 dígitos, 
	 *  sendo o dobro da precisão de variáveis float. 
	 *  Os valores do tipo double são conhecidos como literais de ponto flutuante.
	 *  
	 *  boolean: boolean é um tipo primitivo, ou seja é um número que ocupa 1 byte 
	 *  (ainda que não seja especificado que tenha esse tamanho) e é considerado um 
	 *  tipo por valor, ou seja, seu valor é sua própria identidade, ele existe em si só. 
	 *  A comparação de valores é direta. O valor padrão dele é false.
	 */

}
